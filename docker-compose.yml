version: "3.1"
services:
  backend-data:
    build:
      dockerfile: Dockerfile
      context: ./backend-data
    ports:
      - "5000:5000"
    # TODO: remove next line
    volumes: ["./backend-data:/usr/src/app/"]
